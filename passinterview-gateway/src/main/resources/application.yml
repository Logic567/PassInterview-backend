spring:
  profiles:
    include: routers, jwt

  redis:
    host: 127.0.0.1
    port: 6379

logging:
  level:
    com.logic.passinterview: debug
    org:
      springframework:
        cloud:
          gateway: debug