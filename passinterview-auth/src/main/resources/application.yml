spring:
  profiles:
    include: jwt
  datasource:
    url: jdbc:mysql://192.168.0.192:3306/passinterview_auth?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database: mysql
    show-sql: true

logging:
  level:
    com.logic.passinterview: debug